{
  "name": "{{ productName }}",
  "price": {{ price }},
  "description": "{{ description }}",
  "category": "{{ category }}"
{% if tags %}
  ,"tags": [
{% for tag in tags %}
    "{{ tag }}"{% if not loop.last %},{% endif %}
{% endfor %}
  ]
{% endif %}
}

